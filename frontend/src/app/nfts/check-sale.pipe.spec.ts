import { CheckSalePipe } from './check-sale.pipe';

describe('CheckSalePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckSalePipe();
    expect(pipe).toBeTruthy();
  });
});
